{
	"name": "worker-a",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --outfile=dist/index.js --external:cloudflare:workers",
		"deploy": "wrangler deploy --minify dist/index.js",
		"deploy:staging": "wrangler deploy --minify dist/index.js --env staging",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.6.4",
		"@cloudflare/workers-types": "^4.20250121.0",
		"typescript": "^5.5.2",
		"vitest": "2.1.8",
		"wrangler": "^3.105.0"
	},
	"dependencies": {
		"hono": "^4.6.20",
		"@shared/db-schema": "workspace:*"
	}
}
